── Failure: performAnalyses: calc_differences leads to extra 'diff' parameters ──
nrow(mat_diff) is not strictly more than nrow(mat_no). Difference: 0

── Failure: performAnalyses: calc_differences leads to extra 'diff' parameters ──
any(grepl("diff", rownames(mat_diff))) is not TRUE

`actual`:   FALSE
`expected`: TRUE 

Error:
! Test failed
