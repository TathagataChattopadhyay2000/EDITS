If both chunk.size and scheduling are written in the same call, they are not both used together.

chunk.size has priority.

So:
	•	if chunk.size is given, then R uses chunk.size
	•	scheduling is ignored in that case
	•	scheduling is only used when chunk.size is not given


