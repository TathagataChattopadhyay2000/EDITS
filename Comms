Error in check.evidence.levels(evidence_levels, cohort_names, analyses_list,  : 
  Assertion on 'The 'evidence_levels' and the 'cohort_names' must have the same length' failed: Must be TRUE.
6.
stop(simpleError(sprintf(msg, ...), call.))
5.
mstop("Assertion on '%s' failed: %s.", var.name, res, call. = sys.call(-2L))
4.
makeAssertion(x, res, .var.name, add)
3.
checkmate::assert_true(
length(evidence_levels) == length(cohort_names),
.var.name = "The 'evidence_levels' and the 'cohort_names' must have the same length"
) at CheckingFunctions.R#90
2.
check.evidence.levels(
evidence_levels,
cohort_names,
analyses_list, ... at OCFunctions.R#424
1.
getGoDecisions(analyses_list = an, cohort_names = cohort_names,
evidence_levels = evidence_levels, boundary_rules = quote(c(x[1] >
boundary_rates[1], x[2] > boundary_rates[2], x[3] > boundary_rates[3])),
overall_min_gos = 1)
