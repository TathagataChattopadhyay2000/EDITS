fit_tab <- as.data.frame(mix_fit)

w_nex_derived <- as.numeric(fit_tab["w", ])
mean_one      <- as.numeric(fit_tab["m", ])
sd_one        <- as.numeric(fit_tab["s", ])

w_nex_derived <- w_nex_derived / sum(w_nex_derived)

expect_equal(sum(w_nex_derived), 1, tolerance = 1e-8)
expect_true(all(sd_one > 0))
